name: Email Step Action

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 24ds2000086@ds.study.iitm.ac.in
        run: echo "Hello, world!"
